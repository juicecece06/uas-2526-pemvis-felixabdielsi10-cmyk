//Felix Siadari-12S25043//
//Jusmer Pasaribu-12S25012//


import java.util.*;
import java.lang.Math;

public class SOAL 4 {
    private static Scanner input = new Scanner(System.in);

    public static void main(String[] args) {
        int n, i, shift, total, max, min, rata;
        String nama, namaMax, namaMin;

        total = 0;
        max = -999;
        min = 999;
        n = Integer.parseInt(input.nextLine());
        for (i = 1; i <= n; i++) {
            nama = input.nextLine();
            shift = Integer.parseInt(input.nextLine());
            total = shift;
        }
        rata = (double) total / n;
        System.out.println("SEMUA baris hasil");
    }
}
